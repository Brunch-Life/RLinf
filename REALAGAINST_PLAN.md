# RealAgainst 固定角色 SAC 训练

## 当前路线（2026-09-09）

使用不冻结 ResNet 的 SFT checkpoint（此前无扰动评测 256/256）初始化执行器。
对手和执行器都用 SAC，但每次启动只训练一方，另一方完全固定。
不自动切换角色、不自动组织轮次；手动切换的流程以后再定。
冻结 ResNet、LR=1e-4 的 57.42% 结果保留为独立基线，不是此次初始化。

## GPU 扰动执行修复（2026-09-10）

旧实现只修改 slot 的 GPU 缓冲区，未提交至 PhysX，随后 fetch 又读回静止位姿。
已确认旧对手训练至 34000 步及其 128 视频评估没有真实 slot 扰动；
这些结果不能用作物理对抗效果或抗扰性的证据，原日志/checkpoint 保留追溯。
后续应重新训练对手，不能把旧 replay 当作真实扰动数据继续使用；静态任务的 SFT 权重保留。

修复位于自己的 PegSlotEnv：物理步进前提交刚体数据，步进并 fetch 后读取真实 slot 位姿，
再计算运动量、预算和特权观测。reset 继续使用原有 GPU 初始化流程。
验证包括真实位移、零/耗尽预算、空间边界、部分 reset，以及禁用提交时不得虚增统计的负对照。

## 刚性 peg 正式环境（2026-09-11）

PegSlotEnv 默认使用任务专用 Panda：peg 碰撞和视觉几何并入 panda_hand，
不再依靠夹爪摩擦抓握。保持原尺寸、TCP 偏移和 38.88 g 载荷，
合并质心与惯量；两指物理坐标固定为 0.008 m，slot 仍独立移动。
9D 环境动作、双 RGB＋9D qpos、19D 对手状态和 37D 执行器 critic 接口不变。
只禁用 peg 与夹爪的内部接触，保留 peg 与 slot/桌面等外部碰撞。

该改动移除了滑移/掉落，并使 peg 继承机器人重力补偿，属于新的任务物理。
原自由 peg 的结果不可直接混为同一实验；旧 SFT/对手权重仍可作初始化。
checkpoint 和结果文件标记 environment_revision=rigid_peg_v1，
旧物理版本的 replay/优化器禁止直接 resume；只迁移权重应新开运行。
旧包含独立 actors.peg 的仿真状态也不再兼容。

刚性原型在固定三卡、288 回合下，50k 对手各常用完整 reset 方式均为
221/288（76.74%）；5k 新进程/历史序列均为 273/288（94.79%）。
这不是所有初始化路径都逐值相同的保证：随机数分组与 FK/渲染精度差异仍在，
部分接触轨迹也可能不同。训练内和独立评估继续固定相同批次、seed 和 reset 口径。

正式实现与原型的 288 回合逐步图像、动作和状态完全一致；旧 SFT 无扰动两次均为 96/96。
两份正式 SAC 配置均通过隔离的 4 次 critic、2 次 actor 更新，以及评估/保存回归；
该短测将 critic 预热临时设为 2，正式配置仍为 100，未启动长训。

## 两个独立配置

- realagainst_peg_slot_adversary_sac：固定执行器，训练 19D 特权输入、两层 128 MLP 的对手。
  固定执行器权重在 rollout.robot_model.model_path 中指定；默认使用上述 SFT 模型。
  对手从头初始化；如需只加载已训练对手权重，用 runner.ckpt_path 指定 full_weights.pt。
- realagainst_peg_slot_robot_sac：固定对手，训练双 ResNet18＋四层 256 MLP 的执行器。
  执行器初始化权重在 actor.model.model_path 中指定；默认上述 SFT 模型。
  rollout.adversary_model.model_path 必须手动填写正式选择的对手 checkpoint，不默认使用 smoke 权重。

启动：激活项目 .venv 后，bash examples/embodiment/run_peg_slot_sac.sh <配置名>。
复用独立 Ray head；不改原 SFT 启动脚本。两个配置都使用 GPU0 学习、GPU1–3 采集，
因此默认不能同时启动两个正式实验占用相同 GPU。

## 更新步数

- 每次共 50000 次 SAC critic 更新。
- 前 100 步只更新 critic；第 101 步起更新 actor，正常完成时 actor 共更新 49900 次。
- 每 500 步用最新权重评估一次，评估后只调一次预算，继续同一方训练。
- 每 500 步保存 checkpoint，训练权重每 50 步同步给 rollout。
- update_epoch=1；预算变化不会重置模型、优化器、replay 或训练计数，不重复预热。
- 训练总环境数 96；每环境每次采集 4 步；replay 缓存 32 个批量片段。
- 每次评估 96 环境 ×3 轮=288 episode，复用相同开发种子，不用于最终独立测试。

## 模型与奖励

执行器 actor 只看双 RGB 相机和 9D qpos，输出单步机械臂 6D 动作。
两个 ResNet 和 MLP 都参与更新；SAC 随机动作头以 SFT 动作为中心、小方差初始化。
独立 twin critic 使用 37D 特权状态。对手读取原 17D 特权状态＋总预算＋剩余预算，
输出单步 dx、dy、dyaw。无专家介入，固定对手使用确定性 eval 动作。

- 对手：100 步仍未成功时终局得 1；机器人首次成功终止得 0。
- 执行器：首次成功终止得 1；100 步未成功终止得 0。
- 不叠加旧 ±2 奖励和三项软惩罚；两个终局都不 bootstrap。

## 累计运动预算及动态调整

预算每步花费 = 实际平移路程 / XY参考长度 ＋ 实际转角绝对值 / yaw参考角度。
往返也扣费；先投影至活动边界，再按剩余预算缩小动作，不能透支。
初始 B=1；平移参考长度 5 cm、yaw参考角度 30°，两者共享预算。
保留 reset 相对 XY 半径 5 cm、yaw ±30°，单步每轴 XY 2 mm（对角最多 2.83 mm）和 yaw 1°。

每 500 步根据机器人成功率调整：
低于 45% 时 ×0.8，高于 55% 时 ×1.2，中间不变，常规范围 [0.05, 4]。
新预算只在新 episode 生效，旧 episode 的总预算和余额保持不变，不中途充值。
replay 中每条样本保留该 episode 自己的预算信息，可继续复用不同预算下的数据。
预算用尽后 slot 停在当前位置，机器人继续执行。

如果预算已到下限而成功率仍低于目标，额外检查零预算：
零预算也低于 45% 就保存并暂停；否则暂时回退到零预算。
这是保守保护，不保证预算与成功率单调，也不保证一定能调到 50%。

## 保存与恢复

checkpoint 同时保存模型、优化器、replay、真实 actor/critic 更新次数、下一回合预算和环境版本。
runner.resume_dir 指定同一角色、同一固定对手的 checkpoint 目录可恢复；
新启动环境从 reset 开始，不承诺逐物理帧重现中断时状态。
预算变化记录于 budget_history.json。切换对手时应新开训练，不能冒充同一实验恢复。
旧自动轮换配置已移出工作区备份，不再作为启动入口。
