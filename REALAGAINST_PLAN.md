# RealAgainst 后续规划

## 目标

训练一个控制 slot 扰动的对抗策略，并将机器人策略成功率维持在约 50%，从而产生难度适中的数据，用于下一轮 VLA 学习。

## 当前进展

- 已实现垂直插孔环境：机器人控制 6D EE，slot 控制 XY 和偏航，共 9D 动作。
- 已采集 50 条专家数据并完成 PI0.5 SFT。
- 当前 SFT checkpoint 在 100 个环境上的成功率为 66%。
- 已完成第一轮 DAgger：聚合 50 条原始专家轨迹和 100 条学生访问轨迹。

## 当前阶段：提升基础策略

先用 DAgger 专家标签继续 SFT，提高无扰动插入成功率。基础策略稳定后再开始 slot 对抗训练，避免把策略自身失败误判为对抗效果。

## 已选方案：Checkpoint Pool

暂不在线约束对抗策略始终保持 50% 成功率。先训练对抗策略并定期保存 checkpoint，再用当前 VLA 逐个评测，选择使成功率最接近 50% 的 checkpoint。

选择原因：

- 实现和调试简单，训练过程稳定。
- 对抗训练与 VLA 训练解耦，结果容易复现。
- 不同 checkpoint 可提供不同难度和扰动模式。

VLA 更新后必须重新评测 pool，因为原来的 50% 对手可能已经过弱。

## 后续流程

1. 完成 DAgger SFT，并评测无扰动成功率。
2. 固定更新后的 VLA，训练控制 slot 的对抗策略。
3. 定期保存对抗策略 checkpoint。
4. 用当前 VLA 评测各 checkpoint 的成功率。
5. 选择成功率最接近 50% 的 checkpoint。
6. 使用该对手采集数据并继续训练 VLA。
7. 更新 VLA 后重新评测 checkpoint pool，进入下一轮。

在线维持 50% 的约束强化学习方案暂时保留，待上述闭环验证有效后再考虑。
