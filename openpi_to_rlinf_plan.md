# RLinf现有功能
包装成gym env
s2s(s指slave m指master，指的是state和action的输入，下同，具体细节派一个agent查一下openpi和x2robot-slave的代码)
动作平滑（时间窗）


# openpi现有功能
vla推理（web socket）
执行同时补充（感觉这样不是好的方案）
动作插值（xyz直接插，rpy变quat再插值并归一化到球面再转回rpy）
（可选）视频推流通道
对机器人运行模式监控
s2m sm2s sm2sm

# 如果迁移过来需要实现什么？
1. openpi的web socket方法不可在rlinf里面用，应该改成和其他rlinf实现方式大致相同的基于ray worker的方式
2. 需要支持不同的控制模式，比如s2m sm2s sm2sm，具体的你去openpi里面看看是什么意思，这个作为可选的后面阶段的内容
3. 视频推流(后面的实现)
4. 抛弃时间窗平滑，改成动作插值



# 迁移计划
1. 先实现目前已有的ckpt在机械臂上的推理，最简单的模式，比如s2s，在已有的ckpt上可以复现原来的效果
2. 接着再实现不同的控制模式
3. 接下来是其他的功能


# 注意事项！！！！！！！！！！！！
1. 不要自作聪明，所有问题不准有假设，必须要查明所有的代码的客观证据，没清楚的地方就一直查，没查清楚的就问我
2. 遵循第一性原理，遵循最优雅的实现方式，不要出现没有必要的fallback和兜底，以及其他ai代码常见的行为，需要兜底的地方都检查代码，确认数据对应的格式，不要加没有必要的注释
3. 尽量和rlinf里其他同级的实现对齐，风格和架构尽量保持一致，便于审查和阅读
4. 每次先出人能听得懂的计划，我批准之后再实现，不要不必要的术语，不要不清楚的地方含混过去
